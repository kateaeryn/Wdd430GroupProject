import { Roboto, Shadows_Into_Light } from 'next/font/google';

const roboto = Roboto({ weight: ['400', '700'], subsets: ['latin'] });
const shadows = Shadows_Into_Light({ weight: '400', subsets: ['latin']});

export { roboto, shadows };
